fprintf(file_ptr, "%5u %d %15s %5u %8.2f %12u %8.2f %12ld %s ",t.teacher_id, name_len, t.name, t.age, t.attendance,
        //                                             t.phone_no, t.salary, t.account_no, t.address);
        // fprintf(file_ptr, "%5u %d %15s %6u %8.2f %12u %6.2f %13u %d %s ", s.student_id, name_len, s.name, s.age, s.attendance,
        //                                                  s.phone_no, s.cgpa, s.register_no, 20, s.address);
        // free(s.name);
        // free(t.name);